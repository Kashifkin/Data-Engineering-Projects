import dlt

customers_rules = {
    "rule_1" : "customer_id IS NOT NULL",
    "rule_2" : "customer_name IS NOT NULL"
}

@dlt.table(
    name = "Customer_stg"
)

@dlt.expect_all(customers_rules)
def Customer_stg():
    df = spark.readStream.table("pyspark.source.customers")
    return df


    